---
import Layout from '../layouts/Layout.astro';

const ctaBuild = 'mailto:support@stackpine.com?subject=Start%20My%20Landing%20Page%20Build';
const ctaTemplate = 'mailto:support@stackpine.com?subject=Send%20Me%20The%20Meta%20Template';
const ctaAudit = 'mailto:support@stackpine.com?subject=I%20Want%20The%20%2447%20Congruence%20Audit';
const ctaDiagnostic = 'mailto:support@stackpine.com?subject=Book%20A%2020-Minute%20Diagnostic';
const ctaRetainer = 'mailto:support@stackpine.com?subject=I%20Want%20The%20Full%20Funnel%20CRO%20Retainer';

const credibilityStats = [
  { label: 'Pages Built', value: '140+' },
  { label: 'Ad Spend Sent To Stackpine Pages', value: '$12.4M' },
  { label: 'Average Conversion Lift', value: '+38%' },
];

const servedGroups = [
  'DTC Ecommerce',
  'Info Products',
  'Coaches & Consultants',
  'SaaS',
  'Local Services',
  'Agencies',
];

const mechanismSections = [
  {
    title: 'Introducing Funnel Congruence',
    body: 'The gap between what your ad promises and what your page delivers is where your money disappears. We close that gap first.',
  },
  {
    title: 'The 3-Second Rule',
    body: 'Visitors decide quickly if they are in the right place. Your page must confirm relevance before attention drops.',
  },
  {
    title: 'The Four Jobs Above The Fold',
    body: 'Confirm relevance, make a specific promise, establish trust, and give one clear next step. We engineer all four.',
  },
  {
    title: 'Message-To-Market Match',
    body: 'A Meta click is not a Google search. Meta traffic is colder, more skeptical, and requires stronger guidance.',
  },
  {
    title: 'Congruence Audit Visual',
    body: 'Mismatched funnel: ad hook, generic page, weak CTA, wasted spend. Congruent funnel: same promise, clear path, higher conversion intent.',
  },
  {
    title: 'What We Actually Build',
    body: 'Framer landing pages, sales pages, and full funnel page sequences with conversion-first section architecture.',
  },
  {
    title: 'Why Framer',
    body: 'Fast launch, easy iteration, and no long developer bottleneck when ad performance needs quick page updates.',
  },
  {
    title: 'The Stack Behind It',
    body: 'AI-assisted drafting, conversion frameworks, and a repeatable build system so each page is fast, coherent, and test-ready.',
  },
];

const tiers = [
  {
    name: 'Tier 1 · Template',
    price: '$147 one-time',
    who: 'For teams who want self-serve speed with a conversion structure built for Meta traffic.',
    cta: 'Get The Template',
    href: ctaTemplate,
    features: [
      'Hero and hook layout designed for cold paid clicks',
      'Social proof and trust section sequence',
      'Objection handling structure',
      'Mobile-first CTA placements',
      'Copy prompts for each core section',
    ],
  },
  {
    name: 'Tier 2 · Done-For-You Build',
    price: '$2,500 fixed',
    who: 'For teams that want a complete landing page from brief to live without execution overhead.',
    cta: 'Start Your Build',
    href: ctaBuild,
    features: [
      'Custom copy direction and rewrite',
      'Framer design and build',
      'Mobile optimization and speed pass',
      'Ad-to-page congruence check',
      'One revision round and handoff',
    ],
  },
  {
    name: 'Tier 3 · Full Funnel + CRO',
    price: '$3,500/mo retainer',
    who: 'For brands that want ongoing conversion growth from paid traffic, not one-time page delivery.',
    cta: 'Book A Diagnostic Call',
    href: ctaRetainer,
    features: [
      'Everything in Tier 2',
      'Monthly CRO audit and hypothesis plan',
      'Split test implementation support',
      'GA + Clarity reporting',
      'Monthly strategy call',
    ],
  },
];

const proofOne = [
  {
    type: 'Video Testimonial',
    title: 'DTC Founder · Conversion Lift',
    body: '"We moved from 1.7% to 3.1% conversion in two weeks after the rebuild. Same ad account. Different page flow."',
  },
  {
    type: 'Written Testimonial',
    title: 'Info Product Brand',
    body: '"Our CTR was fine. ROAS was flat. Stackpine fixed page congruence and our CPA dropped 29%."',
  },
  {
    type: 'Written Testimonial',
    title: 'Service Business',
    body: '"Traffic doubled over quarter. We finally had a page that could handle it without leaking qualified leads."',
  },
  {
    type: 'Case Study Teaser',
    title: 'Supplement Brand',
    body: 'Before: 1.4% CVR. After: 2.8% CVR. Main change: clearer promise and stronger above-the-fold CTA path.',
  },
  {
    type: 'Case Study Teaser',
    title: 'Coaching Offer',
    body: 'Before: $122 CPL. After: $79 CPL. Main change: objection timing and simplified form path.',
  },
  {
    type: 'Video Testimonial',
    title: 'Media Buyer',
    body: '"Client retention improved because results stabilized after page congruence was fixed."',
  },
];

const objections = [
  {
    q: 'We already have a developer.',
    a: 'Developers build pages. We engineer paid-traffic conversion sequences. We complement your dev team, not replace them.',
  },
  {
    q: 'Our agency handles the creative.',
    a: 'Most agencies optimize ads, not the post-click conversion path. That is the gap we close.',
  },
  {
    q: 'We tried new pages before and it did not move the needle.',
    a: 'Most redesigns change visuals without fixing strategy. We map intent first, then build the sequence.',
  },
  {
    q: 'We cannot afford it right now.',
    a: 'At active ad spend, even a 1-point conversion increase usually pays for this quickly. Inaction has a recurring cost.',
  },
  {
    q: 'Will this work in our industry?',
    a: 'Yes. The framework is built for paid-traffic behavior across ecommerce, SaaS, coaching, and service offers.',
  },
  {
    q: 'How long does this take?',
    a: 'Template: instant. Done-for-you build: 5-7 business days. Retainer: first optimization cycle starts in 30 days.',
  },
  {
    q: 'What if we do not like the design?',
    a: 'You get preview checkpoints and one structured revision round. Framer enables fast iteration.',
  },
  {
    q: 'We do not have copy ready.',
    a: 'That is normal. Our brief process extracts offer detail and we shape the copy system from there.',
  },
  {
    q: 'Can we do this ourselves?',
    a: 'Yes, use Tier 1. If you want speed and quality under live ad pressure, use Tier 2 or Tier 3.',
  },
  {
    q: 'What if we are not sure the offer is the issue?',
    a: 'Start with the $47 audit. We identify conversion leaks before you commit to a build.',
  },
];

const process = [
  {
    step: '01',
    title: 'Brief and onboarding',
    body: 'You share your offer, audience, ad angles, and current conversion data.',
  },
  {
    step: '02',
    title: 'Congruence mapping',
    body: 'We map your best ad to the current page and identify conversion gaps.',
  },
  {
    step: '03',
    title: 'Design and build',
    body: 'Figma concept, Framer implementation, mobile-first conversion structure.',
  },
  {
    step: '04',
    title: 'Review and approve',
    body: 'Loom walkthrough, one revision pass, and fast approval loop.',
  },
  {
    step: '05',
    title: 'Live and measure',
    body: 'Publish, install tracking clarity, and set baseline conversion metrics.',
  },
  {
    step: '06',
    title: 'Optimize',
    body: 'Retainer clients move into monthly audits and split testing cycles.',
  },
];

const comparisons = [
  {
    against: 'Hiring a general developer',
    body: 'Developers ship what they are asked. They usually are not modeling paid-click psychology or conversion sequencing.',
  },
  {
    against: 'Having your agency add pages',
    body: 'Agencies are often accountable for ad metrics. Post-click architecture gets less focus and slower iteration.',
  },
  {
    against: 'DIY in page builders',
    body: 'Tools without strategy create pages without conversion logic. Templates help, but framework matters most.',
  },
  {
    against: 'The Stackpine difference',
    body: 'We sit at the intersection of Meta ad behavior, conversion psychology, and Framer execution speed.',
  },
];

const proofTwoStats = [
  { label: 'Average CVR Lift', value: '+38%' },
  { label: 'Total Ad Spend Routed Through Our Pages', value: '$12.4M' },
  { label: 'Average Brief-To-Live', value: '6 days' },
];

const proofTwoQuotes = [
  '"We cycled through two agencies before this. Stackpine fixed the page strategy in one sprint." — Founder, DTC brand',
  '"Client retention went up because we finally had consistent post-click performance." — Media Buyer',
  'Case Study: $42k ad spend month. CVR from 1.6% to 3.0%. ROAS from 1.9 to 3.1 after rebuild and split tests.',
];

const faqs = [
  {
    q: 'Do you write copy or only build design?',
    a: 'We do both. Conversion copy and Framer build are integrated so message and layout stay congruent.',
  },
  {
    q: 'What do you need from us to start?',
    a: 'Your current page, ad examples, offer details, and any conversion data you have.',
  },
  {
    q: 'How do you measure success?',
    a: 'Primary: conversion rate and cost per acquisition. Secondary: form completion, scroll depth, and CTA clicks.',
  },
  {
    q: 'Can you follow existing brand guidelines?',
    a: 'Yes. We adapt the conversion system to your brand while keeping performance priorities clear.',
  },
  {
    q: 'Do you support after launch?',
    a: 'Yes. Project clients can add support. Retainer clients get ongoing monthly optimization.',
  },
  {
    q: 'Which industries do you work with?',
    a: 'Ecommerce, info products, SaaS, agencies, local services, and coaching offers.',
  },
  {
    q: 'Template vs done-for-you build?',
    a: 'Template is self-serve. Done-for-you is custom strategy, copy, and build shipped by our team.',
  },
];

const footerLinks = [
  { label: 'Templates', href: '#offers' },
  { label: 'Builds', href: '#offers' },
  { label: 'Retainers', href: '#offers' },
  { label: 'Case Studies', href: '#proof-1' },
  { label: 'Contact', href: 'mailto:support@stackpine.com' },
];
---

<Layout
  title="Stackpine | Meta Ad Landing Pages That Convert"
  description="Stackpine builds Framer landing pages for Meta ad traffic. Clear message match, low cognitive load, and conversion-first funnels."
>
  <header class="sticky top-0 z-50 border-b border-separator bg-background/90 backdrop-blur-xl">
    <nav class="mx-auto flex h-14 max-w-6xl items-center gap-4 px-6">
      <a href="/" class="font-semibold tracking-tight">Stackpine</a>
      <a href="#mechanism" class="text-sm text-tertiary transition-colors hover:text-black">Mechanism</a>
      <a href="#offers" class="text-sm text-tertiary transition-colors hover:text-black">Offers</a>
      <a href="#proof-1" class="text-sm text-tertiary transition-colors hover:text-black">Proof</a>
      <a href="#faq" class="text-sm text-tertiary transition-colors hover:text-black">FAQ</a>
      <a href={ctaBuild} class="ml-auto inline-flex h-8 items-center rounded-full bg-black px-4 text-sm font-medium text-white transition-colors hover:bg-black/80">
        Start Build
      </a>
    </nav>
  </header>

  <main class="mx-auto max-w-6xl px-6 pb-28">
    <section class="border-b border-separator py-16 sm:py-24">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Framer Pages For Meta Traffic</p>
      <h1 class="mt-4 max-w-5xl text-4xl font-semibold leading-[1.02] tracking-tight sm:text-5xl lg:text-7xl">
        Your Meta ads are working. Your landing page is not.
      </h1>
      <p class="mt-6 max-w-3xl text-base text-secondary sm:text-xl">
        We build Framer landing pages engineered for cold Meta traffic so more clicks turn into customers.
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <a href={ctaBuild} class="inline-flex items-center justify-center rounded-full btn-primary px-7 py-3 text-sm font-semibold text-white sm:text-base">
          Start Your Build
        </a>
        <a href={ctaAudit} class="inline-flex items-center justify-center rounded-full border border-separator bg-white px-7 py-3 text-sm font-semibold transition-colors hover:bg-black/5 sm:text-base">
          Get $47 Audit
        </a>
      </div>
      <p class="mt-4 text-sm text-tertiary">Join 200+ brands who stopped leaking ad spend.</p>
    </section>

    <section class="border-b border-separator py-14">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Pattern Interrupt</p>
      <div class="mt-5 space-y-4 text-base text-secondary sm:text-xl">
        <p>The average Meta advertiser loses 73% of clicks before a conversion decision is even made.</p>
        <p>It is not your ad. It is not your audience. It is what happens in the 3 seconds after the click.</p>
        <p>Good CTR, bad ROAS. High traffic, low conversions. Agency blaming creative. Developer blaming copy.</p>
        <p>Your page was built to look good, not to convert a skeptic at midnight on their phone.</p>
      </div>
    </section>

    <section class="border-b border-separator py-14" id="credibility">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Credibility</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">Built for one problem: paid clicks that do not convert.</h2>
      <p class="mt-5 max-w-3xl text-base text-secondary sm:text-lg">
        Stackpine exists to fix the post-click gap for performance marketers. We combine CMO-level messaging, CRO-level structure, and fast Framer execution.
      </p>

      <div class="mt-8 border-t border-separator pt-6">
        <p class="text-sm font-semibold">Founder: Cam</p>
        <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">
          Built for Meta advertisers by someone who understands both sides of the funnel: ad intent before the click and conversion behavior after it.
        </p>
      </div>

      <div class="mt-8 grid gap-6 border-t border-separator pt-6 sm:grid-cols-3">
        {credibilityStats.map((stat) => (
          <div>
            <p class="text-3xl font-semibold tracking-tight sm:text-4xl">{stat.value}</p>
            <p class="mt-1 text-sm text-secondary">{stat.label}</p>
          </div>
        ))}
      </div>

      <div class="mt-8 border-t border-separator pt-6">
        <p class="text-sm font-semibold">Industries served</p>
        <div class="mt-3 flex flex-wrap gap-2">
          {servedGroups.map((group) => (
            <span class="rounded-full border border-separator px-3 py-1.5 text-xs text-secondary sm:text-sm">{group}</span>
          ))}
        </div>
      </div>

      <div class="mt-8 border-t border-separator pt-6">
        <p class="text-base text-secondary sm:text-lg">
          Featured in client podcasts and paid traffic communities. Most powerful signal: clients reporting measurable conversion improvement after launch.
        </p>
      </div>
    </section>

    <section id="mechanism" class="border-b border-separator py-14">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">The Mechanism</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">Funnel congruence: the system behind our page builds</h2>
      <div class="mt-7">
        {mechanismSections.map((section, index) => (
          <div class="border-t border-separator py-5">
            <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">{String(index + 13).padStart(2, '0')}</p>
            <h3 class="mt-2 text-xl font-semibold tracking-tight sm:text-2xl">{section.title}</h3>
            <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">{section.body}</p>
          </div>
        ))}
      </div>
    </section>

    <section id="offers" class="border-b border-separator py-14">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Offer Stack</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">Three ways to work with Stackpine</h2>
      <div class="mt-8">
        {tiers.map((tier, index) => (
          <div class="border-t border-separator py-7">
            <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">{String(index + 21).padStart(2, '0')}</p>
            <div class="mt-2 flex flex-wrap items-center gap-x-4 gap-y-1">
              <h3 class="text-2xl font-semibold tracking-tight sm:text-3xl">{tier.name}</h3>
              <p class="text-sm font-semibold text-[rgb(17,168,255)]">{tier.price}</p>
            </div>
            <p class="mt-3 max-w-3xl text-base text-secondary sm:text-lg">{tier.who}</p>
            <ul class="mt-4 grid gap-2 text-sm text-secondary sm:grid-cols-2 sm:text-base">
              {tier.features.map((feature) => (
                <li>{feature}</li>
              ))}
            </ul>
            <a href={tier.href} class="mt-5 inline-flex items-center rounded-full border border-separator bg-white px-5 py-2.5 text-sm font-semibold transition-colors hover:bg-black/5">
              {tier.cta}
            </a>
          </div>
        ))}
      </div>
    </section>

    <section id="proof-1" class="border-b border-separator py-14">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Social Proof</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">Do not take our word for it. Here is what happens when a page stops leaking.</h2>
      <div class="mt-7">
        {proofOne.map((item, index) => (
          <div class="border-t border-separator py-5">
            <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">{String(index + 31).padStart(2, '0')} · {item.type}</p>
            <h3 class="mt-2 text-xl font-semibold tracking-tight sm:text-2xl">{item.title}</h3>
            <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">{item.body}</p>
          </div>
        ))}
      </div>
      <p class="border-t border-separator pt-5 text-sm text-secondary">4.9/5 average client rating across 60+ submitted reviews.</p>
    </section>

    <section class="border-b border-separator py-14" id="objections">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Objection Handling</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">We know what you are thinking.</h2>
      <div class="mt-7">
        {objections.map((item, index) => (
          <div class="border-t border-separator py-5">
            <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">{String(index + 40).padStart(2, '0')}</p>
            <h3 class="mt-2 text-xl font-semibold tracking-tight sm:text-2xl">{item.q}</h3>
            <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">{item.a}</p>
          </div>
        ))}
      </div>
      <a href={ctaDiagnostic} class="mt-7 inline-flex items-center rounded-full btn-primary px-6 py-2.5 text-sm font-semibold text-white">
        Still unsure? Book a 20-minute diagnostic
      </a>
    </section>

    <section class="border-b border-separator py-14" id="process">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Process</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">What working with us looks like</h2>
      <div class="mt-7">
        {process.map((item, index) => (
          <div class="grid gap-4 border-t border-separator py-5 md:grid-cols-[72px_1fr] md:gap-8">
            <p class="text-sm font-semibold text-[rgb(17,168,255)]">{item.step}</p>
            <div>
              <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">{String(index + 51).padStart(2, '0')}</p>
              <h3 class="mt-2 text-xl font-semibold tracking-tight sm:text-2xl">{item.title}</h3>
              <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">{item.body}</p>
            </div>
          </div>
        ))}
      </div>
      <a href={ctaBuild} class="mt-7 inline-flex items-center rounded-full btn-primary px-6 py-2.5 text-sm font-semibold text-white">Start The Process</a>
    </section>

    <section class="border-b border-separator py-14" id="comparison">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Comparison</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">Three alternatives most teams try first</h2>
      <div class="mt-7">
        {comparisons.map((item, index) => (
          <div class="border-t border-separator py-5">
            <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">{String(index + 60).padStart(2, '0')}</p>
            <h3 class="mt-2 text-xl font-semibold tracking-tight sm:text-2xl">{item.against}</h3>
            <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">{item.body}</p>
          </div>
        ))}
      </div>
      <a href={ctaBuild} class="mt-7 inline-flex items-center rounded-full border border-separator bg-white px-6 py-2.5 text-sm font-semibold transition-colors hover:bg-black/5">
        Make The Right Call For Your Funnel
      </a>
    </section>

    <section class="border-b border-separator py-14" id="proof-2">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Results Wall</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">Numbers that speak for themselves</h2>
      <div class="mt-7 grid gap-6 sm:grid-cols-3">
        {proofTwoStats.map((stat, index) => (
          <div class="border-t border-separator pt-5">
            <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">{String(index + 66).padStart(2, '0')}</p>
            <p class="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">{stat.value}</p>
            <p class="mt-1 text-sm text-secondary">{stat.label}</p>
          </div>
        ))}
      </div>
      <div class="mt-8">
        {proofTwoQuotes.map((quote, index) => (
          <p class="border-t border-separator py-4 text-base text-secondary sm:text-lg">{String(index + 69).padStart(2, '0')} · {quote}</p>
        ))}
      </div>
      <p class="border-t border-separator pt-5 text-sm text-secondary">Trust badges: Framer-ready workflow, analytics setup standards, privacy-first tracking implementation.</p>
    </section>

    <section class="border-b border-separator py-14" id="vision">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Vision</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">What your funnel looks like when it is fixed</h2>
      <div class="mt-6 space-y-4 text-base text-secondary sm:text-lg">
        <p>You stop guessing. Your paid traffic moves through a predictable page sequence built to convert skepticism into action.</p>
        <p>When conversion improves from 1.5% to 4%, CAC drops, ROAS rises, and you can outspend slower competitors.</p>
        <p>Every month on a broken page compounds loss. Every month on a congruent page compounds learning and revenue.</p>
        <p>Meta gets more expensive. Buyers get more skeptical. Brands that win solve conversion now, not later.</p>
      </div>
      <a href={ctaBuild} class="mt-7 inline-flex items-center rounded-full btn-primary px-6 py-2.5 text-sm font-semibold text-white">
        Build This Funnel With Us
      </a>
    </section>

    <section id="faq" class="border-b border-separator py-14">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">FAQ</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">Quick answers before you decide</h2>
      <div class="mt-7">
        {faqs.map((item, index) => (
          <div class="border-t border-separator py-5">
            <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">{String(index + 79).padStart(2, '0')}</p>
            <h3 class="mt-2 text-xl font-semibold tracking-tight sm:text-2xl">{item.q}</h3>
            <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">{item.a}</p>
          </div>
        ))}
      </div>
    </section>

    <section class="border-b border-separator py-14" id="final-push">
      <p class="text-xs font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)] sm:text-sm">Final Conversion Push</p>
      <h2 class="mt-3 max-w-4xl text-3xl font-semibold tracking-tight sm:text-4xl">Your ads deserve a page that works as hard as they do.</h2>

      <div class="mt-7 space-y-6">
        <div class="border-t border-separator pt-5">
          <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">87 · Audit Entry</p>
          <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">Not ready for a build? Find your biggest funnel leak in 60 seconds with the $47 congruence audit.</p>
          <a href={ctaAudit} class="mt-4 inline-flex rounded-full border border-separator bg-white px-5 py-2.5 text-sm font-semibold transition-colors hover:bg-black/5">Get The $47 Audit</a>
        </div>

        <div class="border-t border-separator pt-5">
          <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">88 · Template Entry</p>
          <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">Want speed and control? Use the conversion-engineered Meta template and launch fast.</p>
          <a href={ctaTemplate} class="mt-4 inline-flex rounded-full border border-separator bg-white px-5 py-2.5 text-sm font-semibold transition-colors hover:bg-black/5">Get Template</a>
        </div>

        <div class="border-t border-separator pt-5">
          <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">89 · Done-For-You Build</p>
          <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">Need it done right and shipped fast? We handle copy, structure, and Framer implementation end-to-end.</p>
          <a href={ctaBuild} class="mt-4 inline-flex rounded-full btn-primary px-5 py-2.5 text-sm font-semibold text-white">Start Your Build</a>
        </div>

        <div class="border-t border-separator pt-5">
          <p class="text-xs font-semibold uppercase tracking-[0.1em] text-tertiary">90 · Retainer</p>
          <p class="mt-2 max-w-3xl text-base text-secondary sm:text-lg">Want compounding performance? Move into monthly CRO testing and iteration support.</p>
          <a href={ctaRetainer} class="mt-4 inline-flex rounded-full border border-separator bg-white px-5 py-2.5 text-sm font-semibold transition-colors hover:bg-black/5">Book Diagnostic Call</a>
        </div>
      </div>

      <div class="mt-8 border-t border-separator pt-6">
        <p class="text-sm text-secondary">Capacity note: we onboard a limited number of done-for-you builds per month to maintain implementation quality.</p>
        <p class="mt-2 text-sm text-secondary">Assurance: every build includes a structured revision pass and implementation clarity checklist.</p>
        <p class="mt-2 text-base text-secondary sm:text-lg">Final proof: "We were skeptical. Two weeks later, the page was our top conversion asset."</p>
      </div>

      <a href={ctaBuild} class="mt-8 inline-flex items-center justify-center rounded-full btn-primary px-8 py-3 text-sm font-semibold text-white sm:text-base">
        Fix My Funnel Landing Page
      </a>
    </section>
  </main>

  <footer class="border-t border-separator py-10">
    <div class="mx-auto max-w-6xl px-6">
      <p class="text-sm text-secondary">Remote-first web development agency built for performance marketers.</p>
      <div class="mt-5 flex flex-wrap gap-x-6 gap-y-2 text-sm">
        {footerLinks.map((link) => (
          <a href={link.href} class="text-secondary transition-colors hover:text-black">{link.label}</a>
        ))}
      </div>
      <p class="mt-5 text-sm text-secondary">★ 4.9/5 · 60+ reviews · Meta traffic focused workflow</p>
      <div class="mt-4 flex flex-wrap gap-x-5 gap-y-2 text-xs text-tertiary">
        <a href="mailto:support@stackpine.com?subject=Privacy%20Policy">Privacy Policy</a>
        <a href="mailto:support@stackpine.com?subject=Terms%20Of%20Service">Terms</a>
        <span>Security-first implementation standards</span>
      </div>
    </div>
  </footer>

  <div class="fixed inset-x-0 bottom-0 z-40 border-t border-separator bg-background/95 backdrop-blur-xl">
    <div class="mx-auto flex max-w-6xl items-center justify-between gap-3 px-4 py-3 sm:px-6">
      <p class="text-xs text-secondary sm:text-sm">Not ready for full build? Start with the $47 congruence audit.</p>
      <a href={ctaAudit} class="inline-flex items-center rounded-full btn-primary px-4 py-2 text-xs font-semibold text-white sm:text-sm">Get Audit</a>
    </div>
  </div>
</Layout>
