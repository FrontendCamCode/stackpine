---
import Layout from '../layouts/Layout.astro';

const primaryCta = 'mailto:support@stackpine.com?subject=I%20Want%20The%20%2497%20Funnel%20Clarity%20Build';
const secondaryCta = 'mailto:support@stackpine.com?subject=Send%20Me%20The%20Full%20Build%20Scope';

const offer = {
  name: '$97 Funnel Clarity Build',
  audience: 'Meta ad runners who need more leads, buyers, or booked calls from paid traffic',
  turnaround: '48 hours',
  outcome: 'A clearer landing page structure and copy system that keeps ad intent aligned from click to CTA.',
};

const deliverables = [
  'Headline and subheadline rewrite tied directly to your ad promise',
  'Offer positioning block with one problem, one promise, one next step',
  'Conversion-first section order for low cognitive load',
  'CTA hierarchy map so every section supports the same decision',
  'Objection handling copy for trust, urgency, and clarity',
  'Implementation notes your dev or VA can ship quickly',
  '100-point conversion checklist score so you know exactly what to improve',
];

const process = [
  {
    step: '01',
    title: 'Intake and traffic context',
    body: 'You send your current page, ad angles, and the action you want visitors to take. We review your traffic intent before we touch copy.',
  },
  {
    step: '02',
    title: 'Message congruence rewrite',
    body: 'We rewrite the key sections so the page mirrors ad language and removes confusion in the first scroll depth.',
  },
  {
    step: '03',
    title: 'Structure and decision flow',
    body: 'We reorder sections to answer the visitor\'s next question in sequence: relevance, mechanism, trust, then action.',
  },
  {
    step: '04',
    title: 'Launch-ready handoff',
    body: 'You receive your new copy, section map, CTA plan, and implementation instructions ready for your site stack.',
  },
];

const principles = [
  {
    title: 'Clear, not clever',
    text: 'Your visitor should understand your offer in under 10 seconds. No jargon. No vague positioning.',
  },
  {
    title: 'One path per page',
    text: 'Every section points to one main action so your paid traffic is not split across conflicting asks.',
  },
  {
    title: 'Ad-to-page consistency',
    text: 'If your ad says one thing and your page says another, conversion drops. We fix that mismatch first.',
  },
  {
    title: 'Speed over perfection theater',
    text: 'You need an offer page that can go live, get data, and improve. We prioritize shippable conversion wins.',
  },
];

const checkpoints = [
  {
    category: 'Message Match',
    points: ['Hook alignment', 'Headline relevance', 'Promise consistency', 'Audience specificity', 'Intent continuity'],
  },
  {
    category: 'Offer Clarity',
    points: ['Problem clarity', 'Mechanism clarity', 'Deliverable clarity', 'Timeline clarity', 'Outcome clarity'],
  },
  {
    category: 'Decision Flow',
    points: ['Section sequence', 'Question progression', 'Transition quality', 'CTA timing', 'Scroll momentum'],
  },
  {
    category: 'Trust Layer',
    points: ['Specificity', 'Risk reversal', 'Scope transparency', 'Expectation setting', 'Proof placement'],
  },
  {
    category: 'CTA System',
    points: ['Primary CTA clarity', 'Secondary CTA support', 'Button language', 'Frequency balance', 'Action reinforcement'],
  },
  {
    category: 'UX Simplicity',
    points: ['Reading ease', 'Hierarchy strength', 'Visual pacing', 'Mobile clarity', 'Friction reduction'],
  },
  {
    category: 'Paid Traffic Fit',
    points: ['Cold traffic readiness', 'Warm lead handling', 'Offer temperature', 'Attention retention', 'Click-value protection'],
  },
  {
    category: 'Implementation Readiness',
    points: ['Dev clarity', 'Section specs', 'Copy completeness', 'Variant suggestions', 'Launch checklist'],
  },
  {
    category: 'Economics Readability',
    points: ['Conversion focus', 'Cost-awareness framing', 'AOV awareness', 'Lead value framing', 'Scale readiness'],
  },
  {
    category: 'Post-Click Experience',
    points: ['Page load logic', 'Narrative coherence', 'Objection timing', 'Offer confidence', 'Final CTA strength'],
  },
];

const fit = [
  'You run Meta ads and want better conversion from existing click volume',
  'You have an offer but your landing page feels unclear or bloated',
  'You want a practical website deliverable, not a strategy-only call',
  'You want fast implementation and a clean testing baseline',
];

const notFit = [
  'You need full brand design before any direct-response page work',
  'You are not currently driving paid traffic',
  'You want ten competing CTAs on one page',
  'You want vague copy that sounds fancy but does not sell',
];

const faqs = [
  {
    q: 'Is this a template?',
    a: 'No. We use your offer, your ad angle, and your conversion goal. The final output is custom to your page and traffic intent.',
  },
  {
    q: 'What do I get for $97?',
    a: 'You get a focused front-end conversion build: rewritten sections, section order, CTA system, objection handling, and a launch-ready implementation brief.',
  },
  {
    q: 'Do you also implement the page?',
    a: 'Yes. If you want done-for-you implementation, request the full build scope and we will quote based on stack and speed.',
  },
  {
    q: 'How fast can we start?',
    a: 'Most projects start within one business day once we have your page URL, ad examples, and core offer details.',
  },
  {
    q: 'Will this guarantee ROAS?',
    a: 'No honest CRO team can guarantee platform outcomes. We guarantee a clearer, more coherent page structure designed to improve conversion potential.',
  },
];
---

<Layout
  title="Stackpine | Funnel Websites for Meta Ad Runners"
  description="Stackpine helps Meta ad runners convert more traffic with low-ticket funnel website builds focused on message match, clarity, and conversion flow."
>
  <header class="sticky top-0 z-50 bg-background/88 backdrop-blur-xl border-b border-separator">
    <nav class="max-w-5xl mx-auto px-6 h-14 flex items-center gap-4">
      <a href="/" class="font-semibold tracking-tight">Stackpine</a>
      <a href="#offer" class="text-sm text-tertiary hover:text-black transition-colors">Offer</a>
      <a href="#process" class="text-sm text-tertiary hover:text-black transition-colors">Process</a>
      <a href="#faq" class="text-sm text-tertiary hover:text-black transition-colors">FAQ</a>
      <a href={primaryCta} class="ml-auto text-sm px-4 h-8 rounded-full btn-primary font-medium inline-flex items-center">Get Started</a>
    </nav>
  </header>

  <main class="max-w-5xl mx-auto px-6">
    <section class="py-16 sm:py-24 border-b border-separator">
      <p class="text-xs sm:text-sm font-semibold uppercase tracking-[0.12em] text-[rgb(17,168,255)]">CMO + CRO Web Development</p>
      <h1 class="mt-4 text-4xl sm:text-5xl lg:text-6xl font-semibold tracking-tight leading-[1.03] max-w-4xl">
        We fix the gap between your Meta ad promise and your landing page conversion.
      </h1>
      <p class="mt-6 text-base sm:text-lg text-secondary max-w-3xl">
        If traffic is clicking but not buying, the issue is usually not more ad spend. It is message mismatch, weak offer clarity, and too much friction after the click.
      </p>
      <p class="mt-3 text-base sm:text-lg text-secondary max-w-3xl">
        Stackpine builds low-ticket funnel pages that are easy to understand, fast to implement, and designed to move paid visitors toward one clear action.
      </p>

      <div class="mt-8 flex flex-wrap gap-3">
        <a href={primaryCta} class="text-sm sm:text-base btn-primary text-white px-7 py-3 rounded-full font-semibold inline-flex items-center justify-center">
          Buy The {offer.name}
        </a>
        <a href={secondaryCta} class="text-sm sm:text-base px-7 py-3 rounded-full border border-separator bg-white font-semibold hover:bg-black/5 transition-colors inline-flex items-center justify-center">
          Request Full Build Scope
        </a>
      </div>

      <p class="mt-6 text-sm text-tertiary">
        For: {offer.audience} · Turnaround: {offer.turnaround} · Outcome: {offer.outcome}
      </p>
    </section>

    <section class="py-14 border-b border-separator">
      <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight max-w-4xl">Why most paid traffic pages underperform</h2>
      <div class="mt-6 space-y-4 max-w-3xl text-base sm:text-lg text-secondary">
        <p>The ad says one thing. The page says another. That disconnect breaks trust before your offer is understood.</p>
        <p>The page asks for too much too early. Visitors face multiple paths, unclear outcomes, and weak next-step guidance.</p>
        <p>The copy sounds polished but does not reduce doubt. People leave without taking action, and spend keeps burning.</p>
      </div>
    </section>

    <section id="offer" class="py-14 border-b border-separator">
      <p class="text-xs sm:text-sm font-semibold uppercase tracking-[0.12em] text-[rgb(17,168,255)]">Low-Ticket Front-End Offer</p>
      <h2 class="mt-3 text-3xl sm:text-4xl font-semibold tracking-tight">{offer.name}</h2>
      <p class="mt-4 text-base sm:text-lg text-secondary max-w-3xl">
        This is the fast, practical service that gives you a clearer page architecture you can ship quickly and test against paid traffic.
      </p>

      <div class="mt-8">
        {deliverables.map((item) => (
          <div class="py-3 border-t border-separator text-base sm:text-lg text-secondary">{item}</div>
        ))}
      </div>

      <div class="mt-8 flex flex-wrap gap-3">
        <a href={primaryCta} class="text-sm btn-primary text-white px-6 py-2.5 rounded-full font-semibold inline-flex">Start For {offer.price}</a>
        <a href={secondaryCta} class="text-sm px-6 py-2.5 rounded-full border border-separator bg-white font-semibold hover:bg-black/5 transition-colors inline-flex">
          Need Done-For-You Build
        </a>
      </div>
    </section>

    <section id="process" class="py-14 border-b border-separator">
      <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">How we build it</h2>
      <div class="mt-7">
        {process.map((item) => (
          <div class="py-6 border-t border-separator grid md:grid-cols-[84px_1fr] gap-3 md:gap-6">
            <p class="text-sm font-semibold text-[rgb(17,168,255)]">{item.step}</p>
            <div>
              <h3 class="text-xl sm:text-2xl font-semibold tracking-tight">{item.title}</h3>
              <p class="mt-2 text-base sm:text-lg text-secondary max-w-3xl">{item.body}</p>
            </div>
          </div>
        ))}
      </div>
    </section>

    <section class="py-14 border-b border-separator">
      <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">The standards we use on every page</h2>
      <div class="mt-7">
        {principles.map((item) => (
          <div class="py-5 border-t border-separator">
            <h3 class="text-xl sm:text-2xl font-semibold tracking-tight">{item.title}</h3>
            <p class="mt-2 text-base sm:text-lg text-secondary max-w-3xl">{item.text}</p>
          </div>
        ))}
      </div>
    </section>

    <section class="py-14 border-b border-separator">
      <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">Our 100-point conversion checklist</h2>
      <p class="mt-4 text-base sm:text-lg text-secondary max-w-3xl">
        We score your page across ten categories so you know exactly where conversion friction exists and what to fix first.
      </p>

      <div class="mt-8 grid md:grid-cols-2 gap-x-10 gap-y-7">
        {checkpoints.map((group) => (
          <div>
            <h3 class="text-lg sm:text-xl font-semibold tracking-tight">{group.category}</h3>
            <ul class="mt-3 space-y-2 text-sm sm:text-base text-secondary">
              {group.points.map((point) => (
                <li>{point}</li>
              ))}
            </ul>
          </div>
        ))}
      </div>
    </section>

    <section class="py-14 border-b border-separator grid lg:grid-cols-2 gap-10">
      <div>
        <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">Best fit clients</h2>
        <div class="mt-5">
          {fit.map((item) => (
            <div class="py-3 border-t border-separator text-base sm:text-lg text-secondary">{item}</div>
          ))}
        </div>
      </div>
      <div>
        <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">Not a fit</h2>
        <div class="mt-5">
          {notFit.map((item) => (
            <div class="py-3 border-t border-separator text-base sm:text-lg text-secondary">{item}</div>
          ))}
        </div>
      </div>
    </section>

    <section class="py-14 border-b border-separator">
      <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">Pricing and scope</h2>
      <p class="mt-4 text-base sm:text-lg text-secondary max-w-3xl">
        The front-end service is {offer.price}. You receive a conversion-focused landing page rewrite and structure plan within {offer.turnaround}.
      </p>
      <p class="mt-3 text-base sm:text-lg text-secondary max-w-3xl">
        If you want full implementation, request the done-for-you build scope and we will map timeline, stack, and delivery costs clearly before starting.
      </p>
      <div class="mt-7 flex flex-wrap gap-3">
        <a href={primaryCta} class="text-sm sm:text-base btn-primary text-white px-7 py-3 rounded-full font-semibold inline-flex">Start The {offer.name}</a>
        <a href={secondaryCta} class="text-sm sm:text-base px-7 py-3 rounded-full border border-separator bg-white font-semibold hover:bg-black/5 transition-colors inline-flex">
          Send Me Implementation Quote
        </a>
      </div>
    </section>

    <section id="faq" class="py-14 border-b border-separator">
      <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">Frequently asked questions</h2>
      <div class="mt-6">
        {faqs.map((item) => (
          <div class="py-5 border-t border-separator">
            <h3 class="text-lg sm:text-xl font-semibold tracking-tight">{item.q}</h3>
            <p class="mt-2 text-base sm:text-lg text-secondary max-w-3xl">{item.a}</p>
          </div>
        ))}
      </div>
    </section>

    <section class="py-16 sm:py-20">
      <p class="text-xs sm:text-sm font-semibold uppercase tracking-[0.12em] text-[rgb(17,168,255)]">Final Call</p>
      <h2 class="mt-4 text-3xl sm:text-4xl lg:text-5xl font-semibold tracking-tight leading-tight max-w-4xl">
        You already paid for the click. Now make the page do its job.
      </h2>
      <p class="mt-5 text-base sm:text-lg text-secondary max-w-3xl">
        If you want a cleaner conversion path from Meta ad to buyer action, we can build it with you fast.
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <a href={primaryCta} class="text-sm sm:text-base btn-primary text-white px-7 py-3 rounded-full font-semibold inline-flex items-center justify-center">
          Buy The {offer.name}
        </a>
        <a href={secondaryCta} class="text-sm sm:text-base px-7 py-3 rounded-full border border-separator bg-white font-semibold hover:bg-black/5 transition-colors inline-flex items-center justify-center">
          Talk About Done-For-You Build
        </a>
      </div>
    </section>
  </main>

  <footer class="py-10 border-t border-separator">
    <div class="max-w-5xl mx-auto px-6 flex flex-col sm:flex-row items-start sm:items-center justify-between gap-3">
      <p class="text-sm text-secondary">© {new Date().getFullYear()} Stackpine</p>
      <a href="mailto:support@stackpine.com" class="text-sm font-medium hover:opacity-70 transition-opacity">support@stackpine.com</a>
    </div>
  </footer>
</Layout>
