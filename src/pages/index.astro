---
import Layout from '../layouts/Layout.astro';

const primaryCta = 'mailto:support@stackpine.com?subject=Start%20My%20Landing%20Page%20Build';
const secondaryCta = 'mailto:support@stackpine.com?subject=Send%20Me%20The%20Offer%20Details';

const bullets = [
  'Framer landing page built for cold Meta ad traffic',
  'Clear message match from ad click to on-page offer',
  'Low-friction layout focused on leads, buyers, or booked calls',
];
---

<Layout
  title="Stackpine | Meta Landing Page Offer"
  description="Stackpine builds conversion-focused Framer landing pages for Meta ad traffic."
>
  <main class="min-h-[100svh] flex items-center">
    <section class="w-full">
      <div class="mx-auto max-w-5xl px-6 py-10">
        <p class="text-xs sm:text-sm font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)]">Stackpine · CMO + CRO Web Development</p>

        <h1 class="mt-4 max-w-4xl text-4xl sm:text-5xl lg:text-6xl font-semibold tracking-tight leading-[1.03]">
          Your Meta ads are working. Your landing page is not.
        </h1>

        <p class="mt-6 max-w-3xl text-base sm:text-xl text-secondary">
          We build Framer landing pages engineered to convert paid traffic with clear offer messaging, low cognitive load, and one decisive action path.
        </p>

        <ul class="mt-8 space-y-2.5 max-w-3xl">
          {bullets.map((bullet) => (
            <li class="text-sm sm:text-base text-secondary">{bullet}</li>
          ))}
        </ul>

        <div class="mt-8 flex flex-wrap gap-3">
          <a href={primaryCta} class="inline-flex items-center justify-center rounded-full btn-primary px-7 py-3 text-sm sm:text-base font-semibold text-white">
            Start Your Build
          </a>
          <a href={secondaryCta} class="inline-flex items-center justify-center rounded-full border border-separator bg-white px-7 py-3 text-sm sm:text-base font-semibold transition-colors hover:bg-black/5">
            Get Offer Details
          </a>
        </div>

        <p class="mt-4 text-xs sm:text-sm text-tertiary">Typical first draft in 5-7 business days.</p>
      </div>
    </section>
  </main>
</Layout>
