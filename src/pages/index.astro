---
import Layout from '../layouts/Layout.astro';

const primaryCta = 'mailto:support@stackpine.com?subject=Build%20My%20Low-Ticket%20Funnel%20Website';
const secondaryCta = 'mailto:support@stackpine.com?subject=Send%20Me%20The%20%2497%20Funnel%20Scope';

const clarity = {
  service: 'Low-Ticket Funnel Website Build',
  audience: 'Meta ad runners',
  price: '$97',
  promise: 'A conversion-focused funnel website with clear ad-to-page congruence and low cognitive load.',
};

const themes = [
  {
    name: 'Offer Clarity',
    problem: 'People do not buy when your offer feels vague.',
    method: 'We tighten your message to one clear problem, one clear promise, and one clear CTA.',
    gain: 'Visitors understand your offer in seconds.',
  },
  {
    name: 'Ad to Page Match',
    problem: 'Meta ads lose money when page copy does not match ad intent.',
    method: 'We align ad hook, headline, proof, and CTA language.',
    gain: 'Clicks feel coherent and conversion gets easier.',
  },
  {
    name: 'Low Cognitive Load UX',
    problem: 'Too many choices create hesitation and drop-off.',
    method: 'We design a guided flow with clean hierarchy and minimal decision friction.',
    gain: 'Buyers move faster from interest to action.',
  },
  {
    name: 'Conversion Copy',
    problem: 'Generic copy attracts attention but not purchases.',
    method: 'We write direct-response copy focused on pain, clarity, and believable outcomes.',
    gain: 'More of your traffic turns into buyers.',
  },
  {
    name: 'Checkout Confidence',
    problem: 'Intent dies at checkout when trust is weak.',
    method: 'We place proof, risk reversal, and value stack where decisions happen.',
    gain: 'Checkout completion improves without gimmicks.',
  },
  {
    name: 'Economics and Break-Even',
    problem: 'Scaling ads before math is stable creates expensive chaos.',
    method: 'We build the funnel around CAC, CVR, AOV, and payback clarity.',
    gain: 'You scale from economics, not emotion.',
  },
  {
    name: 'Post-Purchase Flow',
    problem: 'Many buyers disappear after one purchase.',
    method: 'We create simple follow-up paths that keep momentum after checkout.',
    gain: 'Higher customer value and more repeat action.',
  },
  {
    name: 'Testing Rhythm',
    problem: 'Random changes make growth hard to trust.',
    method: 'We prioritize tests based on conversion impact, not opinions.',
    gain: 'A cleaner path to predictable improvement.',
  },
  {
    name: 'Service Positioning',
    problem: 'If everything sounds custom, nothing feels buyable.',
    method: 'We package your low-ticket web build as a clear, easy-to-say-yes service.',
    gain: 'Better offer fit for paid traffic audiences.',
  },
  {
    name: 'Scale Readiness',
    problem: 'Funnels break when volume increases.',
    method: 'We structure your pages and messaging to stay coherent under higher traffic.',
    gain: 'More confidence when ad spend grows.',
  },
];

const lenses = [
  {
    label: 'What You Get',
    text: 'A web development deliverable, not theory. You leave with a build-ready funnel website section set.',
  },
  {
    label: 'Why It Converts',
    text: 'Every block supports one decision path so buyers never wonder what to do next.',
  },
  {
    label: 'Business Impact',
    text: 'Higher funnel clarity usually means better conversion and healthier paid traffic economics.',
  },
  {
    label: 'Speed to Launch',
    text: 'Simple architecture means faster implementation and quicker feedback from real traffic.',
  },
  {
    label: 'Best For',
    text: 'Ad runners who want cleaner conversion, less message drift, and less wasted spend.',
  },
  {
    label: 'Execution Style',
    text: 'CMO strategy and CRO execution in one coherent website build process.',
  },
  {
    label: 'Friction Removed',
    text: 'We remove unnecessary sections, weak transitions, and confusing offer language.',
  },
  {
    label: 'Decision Simplicity',
    text: 'We keep one core promise visible across the full funnel so buyers stay oriented.',
  },
  {
    label: 'Measurement Focus',
    text: 'You can judge progress with clear conversion and efficiency metrics.',
  },
  {
    label: 'Long-Term Value',
    text: 'A coherent funnel website becomes a durable paid traffic asset, not a one-off page.',
  },
];

const sections = Array.from({ length: 100 }, (_, i) => {
  const id = i + 1;
  const theme = themes[Math.floor(i / 10)];
  const lens = lenses[i % 10];

  return {
    id,
    theme,
    lens,
    title: `Section ${String(id).padStart(2, '0')}: ${theme.name}`,
    lead: theme.problem,
    method: theme.method,
    gain: theme.gain,
  };
});

const sectionGroups = Array.from({ length: 10 }, (_, i) => ({
  label: `${i * 10 + 1}-${i * 10 + 10}`,
  anchor: `section-${i * 10 + 1}`,
  title: themes[i].name,
}));
---

<Layout
  title="Stackpine | Low-Ticket Funnel Website Build for Meta Ad Runners"
  description="Stackpine builds low-ticket funnel websites for Meta ad runners with maximum offer clarity, low cognitive load, and conversion-first structure."
>
  <header class="sticky top-0 z-50 bg-background/80 backdrop-blur-lg border-b border-separator">
    <nav class="max-w-5xl mx-auto px-6 h-14 flex items-center">
      <a href="/" class="flex items-center gap-2 font-semibold">
        <span class="size-6 rounded-full bg-black flex items-center justify-center text-white text-xs font-bold">S</span>
        <span>Stackpine</span>
      </a>
      <span class="ml-3 text-tertiary text-xs sm:text-sm">CMO + CRO web development for paid traffic</span>
      <a href={primaryCta} class="ml-auto text-sm px-4 h-8 rounded-lg font-medium btn-primary inline-flex items-center transition-colors">Hire Us</a>
    </nav>
  </header>

  <main class="max-w-5xl mx-auto px-6">
    <section class="py-14 border-b border-separator">
      <p class="text-sm font-semibold text-[rgb(17,168,255)] uppercase tracking-wide">Low-Ticket Service for Ad Runners</p>
      <h1 class="mt-4 text-4xl sm:text-5xl lg:text-6xl font-bold leading-tight tracking-tight">
        We build low-ticket funnel websites that make Meta ad traffic easier to convert.
      </h1>
      <p class="mt-5 text-secondary text-base sm:text-lg max-w-4xl">
        {clarity.service} for {clarity.audience}. {clarity.promise}
      </p>
      <p class="mt-3 text-secondary text-base sm:text-lg max-w-4xl">
        Customer-first focus: simple explanation, coherent flow, and conversion-driven web sections that help visitors buy without confusion.
      </p>

      <div class="mt-6 flex flex-wrap items-center gap-3 text-sm">
        <span class="px-3 py-1.5 rounded-full border border-separator">Service: {clarity.service}</span>
        <span class="px-3 py-1.5 rounded-full border border-separator">Audience: {clarity.audience}</span>
        <span class="px-3 py-1.5 rounded-full border border-separator">Front-end offer: {clarity.price}</span>
      </div>

      <div class="mt-8 flex flex-wrap gap-2.5">
        <a href={primaryCta} class="text-sm sm:text-base btn-primary font-semibold text-white px-7 py-3 rounded-xl tracking-[-0.0125em] inline-flex items-center justify-center transition-colors">
          Hire Stackpine To Build Your Funnel
        </a>
        <a href={secondaryCta} class="text-sm sm:text-base font-semibold px-7 py-3 rounded-xl bg-white tracking-[-0.0125em] inline-flex items-center gap-2 border border-separator hover:bg-black/5 transition-colors">
          Send Me The {clarity.price} Scope
        </a>
      </div>
    </section>

    <section class="py-8 border-b border-separator">
      <p class="font-medium text-sm">Jump by phase</p>
      <div class="mt-3 flex flex-wrap gap-2">
        {sectionGroups.map((group) => (
          <a href={`#${group.anchor}`} class="text-sm px-3 py-1.5 rounded-full border border-separator hover:bg-black/5 transition-colors">
            {group.label} · {group.title}
          </a>
        ))}
      </div>
    </section>

    <section>
      {sections.map((s) => (
        <section id={`section-${s.id}`} class="py-12 border-b border-separator last:border-b-0">
          <div class="flex items-center justify-between gap-3">
            <p class="text-xs uppercase tracking-wide text-tertiary font-semibold">{s.theme.name} · {String(s.id).padStart(2, '0')}</p>
            <a href={primaryCta} class="text-xs sm:text-sm text-[rgb(17,168,255)] font-medium hover:opacity-70 transition-opacity">Hire us for this</a>
          </div>

          <h2 class="mt-3 text-2xl sm:text-3xl font-bold tracking-tight">{s.title}</h2>
          <p class="mt-4 text-secondary text-base sm:text-lg max-w-3xl">{s.lead}</p>
          <p class="mt-2 text-secondary text-base sm:text-lg max-w-3xl">{s.method}</p>
          <p class="mt-2 text-secondary text-base sm:text-lg max-w-3xl">{s.gain}</p>

          <div class="mt-5 grid md:grid-cols-3 gap-5">
            <div>
              <p class="text-xs uppercase tracking-wide text-tertiary">Offer</p>
              <p class="mt-1 text-sm">{clarity.service}</p>
            </div>
            <div>
              <p class="text-xs uppercase tracking-wide text-tertiary">Customer Lens</p>
              <p class="mt-1 text-sm text-secondary">{s.lens.label}: {s.lens.text}</p>
            </div>
            <div>
              <p class="text-xs uppercase tracking-wide text-tertiary">Outcome</p>
              <p class="mt-1 text-sm">{clarity.outcome}</p>
            </div>
          </div>

          {s.id % 5 === 0 && (
            <div class="mt-6 flex flex-wrap gap-2.5">
              <a href={primaryCta} class="text-sm btn-primary text-white px-5 py-2.5 rounded-lg inline-flex">Hire Stackpine</a>
              <a href={secondaryCta} class="text-sm px-5 py-2.5 rounded-lg border border-separator bg-white hover:bg-black/5 inline-flex">Get The Scope</a>
            </div>
          )}
        </section>
      ))}
    </section>

    <section class="py-14 border-t border-separator">
      <p class="text-[rgb(17,168,255)] font-semibold uppercase tracking-wide text-sm">Final CTA</p>
      <h2 class="mt-3 text-3xl sm:text-4xl font-bold tracking-tight">Ready for a clearer, more coherent funnel website?</h2>
      <p class="mt-4 text-secondary text-base sm:text-lg max-w-3xl">
        If you run Meta ads and want more low-ticket buyers through simpler website decisions, we can build it for you.
      </p>
      <div class="mt-6 flex flex-wrap gap-2.5">
        <a href={primaryCta} class="text-sm sm:text-base btn-primary font-semibold text-white px-7 py-3 rounded-xl tracking-[-0.0125em] inline-flex items-center justify-center transition-colors">
          Hire Stackpine Now
        </a>
        <a href={secondaryCta} class="text-sm sm:text-base font-semibold px-7 py-3 rounded-xl bg-white tracking-[-0.0125em] inline-flex items-center gap-2 border border-separator hover:bg-black/5 transition-colors">
          Send Me The Scope
        </a>
      </div>
    </section>
  </main>

  <footer class="w-full py-10">
    <div class="max-w-5xl px-6 mx-auto border-t border-separator pt-8 flex flex-col sm:flex-row justify-between gap-4">
      <span class="text-secondary text-sm">© {new Date().getFullYear()} Stackpine</span>
      <a href="mailto:support@stackpine.com" class="text-sm font-medium hover:opacity-70 transition-opacity">support@stackpine.com</a>
    </div>
  </footer>
</Layout>
