---
import Layout from '../layouts/Layout.astro';

const primaryCta = 'mailto:support@stackpine.com?subject=Show%20Me%20The%20Gap%20In%20My%20Website';
const secondaryCta = 'mailto:support@stackpine.com?subject=How%20Is%20Stackpine%20Different';
---

<Layout
  title="Stackpine | Web Development For Performance Growth"
  description="Stackpine is a web development partner for brands that need better conversion from paid traffic."
>
  <header class="fixed inset-x-0 top-0 z-40 border-b border-separator bg-background/90 backdrop-blur-xl">
    <div class="mx-auto flex h-14 max-w-5xl items-center px-6">
      <p class="text-sm font-semibold tracking-tight">Stackpine</p>
      <div class="ml-auto flex items-center gap-2 sm:gap-3">
        <a href={secondaryCta} class="hidden sm:inline-flex text-sm text-secondary transition-colors hover:text-black">Why We Are Different</a>
        <a href={primaryCta} class="inline-flex items-center justify-center rounded-full btn-primary px-4 py-1.5 text-xs sm:text-sm font-semibold text-white">
          Show Me The Gap
        </a>
      </div>
    </div>
  </header>

  <main class="min-h-[100svh] flex items-center pt-24 pb-28 sm:pb-24">
    <section class="w-full">
      <div class="mx-auto max-w-5xl px-6 py-10">
        <p class="text-xs sm:text-sm font-semibold uppercase tracking-[0.13em] text-[rgb(17,168,255)]">Conversion Focussed Web Development</p>

        <h1 class="mt-4 max-w-4xl text-4xl sm:text-5xl lg:text-6xl font-semibold tracking-tight leading-[1.03]">
          Your ads get attention. Your website decides what happens next.
        </h1>

        <p class="mt-6 max-w-3xl text-base sm:text-xl text-secondary">
          Most teams keep buying clicks and hope the page catches up. We build the missing conversion system inside your website so traffic turns
          into qualified action.
        </p>

        <div class="mt-8 grid max-w-3xl gap-4">
          <div class="flex gap-3 border-t border-separator pt-3">
            <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-tabler icons-tabler-outline icon-tabler-target-arrow mt-0.5 shrink-0" width="22" height="22" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
              <path d="M12 12m-1 0a1 1 0 1 0 2 0a1 1 0 1 0 -2 0" />
              <path d="M12 7a5 5 0 1 0 5 5" />
              <path d="M13.5 3h7.5v7.5" />
              <path d="M21 3l-9 9" />
            </svg>
            <p class="text-sm sm:text-base text-secondary">Most people optimize the ad. We optimize the moment right after the click.</p>
          </div>

          <div class="flex gap-3 border-t border-separator pt-3">
            <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-tabler icons-tabler-outline icon-tabler-code mt-0.5 shrink-0" width="22" height="22" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
              <path d="M7 8l-4 4l4 4" />
              <path d="M17 8l4 4l-4 4" />
              <path d="M14 4l-4 16" />
            </svg>
            <p class="text-sm sm:text-base text-secondary">We rebuild page logic first, so every section naturally pulls visitors to the next step.</p>
          </div>

          <div class="flex gap-3 border-t border-separator pt-3">
            <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-tabler icons-tabler-outline icon-tabler-bolt mt-0.5 shrink-0" width="22" height="22" viewBox="0 0 24 24" stroke-width="1.8" stroke="currentColor" fill="none" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
              <path d="M13 2l-8 12h6l-2 8l8 -12h-6l2 -8" />
            </svg>
            <p class="text-sm sm:text-base text-secondary">Same brand, same offer, different performance. The difference is in how the experience is engineered.</p>
          </div>
        </div>

        <div class="mt-8 flex flex-wrap gap-3">
          <a href={primaryCta} class="inline-flex items-center justify-center rounded-full btn-primary px-7 py-3 text-sm sm:text-base font-semibold text-white">
            Show Me The Gap
          </a>
          <a href={secondaryCta} class="inline-flex items-center justify-center rounded-full border border-separator bg-white px-7 py-3 text-sm sm:text-base font-semibold transition-colors hover:bg-black/5">
            How Are You Different?
          </a>
        </div>

        <p class="mt-4 text-xs sm:text-sm text-tertiary">No locked package. We scope around the exact point your funnel is leaking.</p>
      </div>
    </section>
  </main>

  <div class="fixed inset-x-0 bottom-0 z-40 border-t border-separator bg-background/95 backdrop-blur-xl">
    <div class="mx-auto flex max-w-5xl items-center justify-between gap-3 px-4 py-3 sm:px-6" style="padding-bottom: max(0.75rem, env(safe-area-inset-bottom));">
      <p class="text-xs sm:text-sm text-secondary">Want to see what is quietly killing your conversion rate?</p>
      <a href={primaryCta} class="inline-flex items-center justify-center rounded-full btn-primary px-4 py-2 text-xs sm:text-sm font-semibold text-white whitespace-nowrap">
        Find The Leak
      </a>
    </div>
  </div>
</Layout>
